name: Wake Up Streamlit App
on:
  schedule:
    - cron: '0 */10 * * *' # 10時間おきに実行（UTC基準）
  workflow_dispatch: # ボタンから手動で実行してテストすることも可能に設定

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Send keep-alive request
        run: curl -I https://ms-ailab.streamlit.app/
